name: stackw
publish_to: "none"

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  # ここに他の依存があればこの下に列挙
  # cupertino_icons: ^1.0.8 など

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
